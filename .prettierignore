# Vendored upstream copy — must stay byte-identical to
# deepseek-ai/deepseek-harness for `pnpm run sync --check` to report clean.
external/

# Build output
lib/

# Dependencies / VCS / lockfiles / prose
node_modules/
.git/
*lock*
*.md
LICENSE
