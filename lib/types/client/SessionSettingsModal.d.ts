import * as React from 'react';
import { type ClientPageProps } from './types.ts';
export declare function SessionSettingsModal({ api, t, sessionId, sessionTitle, useSessions, onClose, onSave, }: ClientPageProps): React.DetailedReactHTMLElement<{
    className: string;
}, HTMLElement>;
